<!DOCTYPE html>
<html>
<head>
  <title>Property Map App</title>
  <script src="https://maps.googleapis.com/maps/api/js?key=YOUR_GOOGLE_MAPS_API_KEY"></script>
</head>
<body>
  <h1>Property Information</h1>
  <p><strong>Address:</strong> <%= @property_data[:address] %></p>
  <p><strong>Current Owner:</strong> <%= @property_data[:current_owner] %></p>
  <p><strong>Previous Owner:</strong> <%= @property_data[:previous_owner] %></p>
  <p><strong>Cost:</strong> <%= @property_data[:cost] %></p>
  <div id="map" style="width: 100%; height: 400px;"></div>
  <script>
    var map;
    function initMap() {
      var propertyLocation = { lat: LATITUDE, lng: LONGITUDE }; // Replace with actual coordinates
      map = new google.maps.Map(document.getElementById('map'), {
        center: propertyLocation,
        zoom: 15
      });
      var marker = new google.maps.Marker({
        position: propertyLocation,
        map: map,
        title: 'Property Location'
      });
    }
  </script>
  <script async defer
    src="https://maps.googleapis.com/maps/api/js?key=YOUR_GOOGLE_MAPS_API_KEY&callback=initMap">
  </script>
  <a href="/">Back to Search</a>
</body>
</html>
